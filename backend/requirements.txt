fastapi==0.115.8
uvicorn==0.32.0
gunicorn==22.0.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pydantic==2.9.2
pydantic-settings==2.6.1
pydantic[email]==2.9.2
python-dotenv==1.0.1
PyJWT==2.12.1
passlib[bcrypt]==1.7.4
python-multipart==0.0.17
google-generativeai==0.8.3
httpx==0.27.2
tenacity==8.3.0
python-dateutil==2.9.0
supabase==2.10.0
Pillow==12.1.1
numpy==1.26.4
pytesseract==0.3.10

# Document parsing — PDF
PyMuPDF==1.24.5
pypdf==4.3.1

# Document parsing — DOCX, PPTX
python-docx==1.1.2
python-pptx==1.0.2

# Document parsing — XLSX, XLS, ODS, CSV
openpyxl==3.1.5
xlrd==2.0.1
odfpy==1.4.1

# Document parsing — EPUB, HTML
ebooklib==0.18
beautifulsoup4==4.12.3

# ML / NLP dependencies (required by ml/, ml_models/, ml_engines/)
scikit-learn==1.6.1
pandas==2.2.2
scipy==1.13.1
nltk==3.8.1
sentence-transformers==3.0.1

# CPU-only torch — the +cpu local-version tag only exists on PyTorch's own index
# and cannot be resolved by a plain `pip install`. On Render (no GPU/CUDA),
# the standard PyPI wheel runs in CPU mode automatically.
torch==2.2.2

# Optional heavy ML (only loaded when ENABLE_HEAVY_ML=true or explicitly imported)
# spacy is imported lazily — install separately if needed:
#   pip install spacy && python -m spacy download en_core_web_sm
# bertopic, umap-learn, hdbscan are imported lazily — install separately if needed:
#   pip install bertopic umap-learn hdbscan
